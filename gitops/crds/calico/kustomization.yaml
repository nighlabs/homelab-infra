apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Vendored Calico CRDs (see the header in crds.yaml for why they're vendored and
# how to regenerate them). Plain `resources:` — NOT a kustomize Component, and no
# patches: these are generated output, and the only supported edit is a
# regeneration at a new chart version.
resources:
  - crds.yaml
