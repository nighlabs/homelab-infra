apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Infrastructure controllers, in dependency order as they arrive (design §6):
# calico (CNI) first; then metallb, cert-manager, ceph-csi-operator, ESO, ...
resources:
  - calico
